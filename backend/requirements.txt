# Web framework
fastapi==0.138.0
uvicorn[standard]==0.49.0
python-multipart==0.0.31

# Database
sqlalchemy[asyncio]==2.0.51
asyncpg==0.29.0
alembic==1.18.4
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0  # Pinned: 5.0.0 breaks passlib 1.7.4 compatibility (see PR #52)
authlib==1.6.12
itsdangerous==2.2.0
pyotp>=2.9.0

# Validation
pydantic>=2.9.0,<3.0  # fastapi 0.135.x requires pydantic>=2.9.0
pydantic-settings==2.13.1
email-validator==2.3.0

# Sigma (for future tasks)
pysigma>=1.3.3
pysigma-backend-opensearch>=1.0.0

# MITRE ATT&CK
mitreattack-python>=3.0.0

# GeoIP
geoip2>=5.2.0

# Background scheduling
apscheduler>=3.10.0

# Git config-as-code sync (one-way push of deployed rules to a repo)
GitPython>=3.1.43

# Metrics
prometheus-client>=0.20.0  # Prometheus pipeline metrics (eps, alerts, latency)

# OpenSearch client
opensearch-py==3.2.0

# Redis for distributed locking and queue management
redis>=8.0.0

# Testing
pytest>=8.2,<9  # pytest 9 needs pytest-asyncio 1.x migration; kept on 8.x
pytest-asyncio==0.23.6
httpx==0.28.1
aiosqlite==0.20.0

# Dev tools
ruff==0.15.8

# Security
nh3>=0.3.5  # Fast HTML sanitization (binding to Rust ammonia)
regex>=2024.11.6  # Timeout-capable regex engine (ReDoS protection on the ingest hot path)

# Reporting
reportlab>=4.2.0  # PDF generation

# Logging
structlog>=26.1.0  # Structured logging
python-json-logger>=4.1.0  # JSON logging fallback

# Type checking
mypy>=1.9.0  # Static type checker
types-requests>=2.33.0.20260518  # Type stubs for requests
